# Frontend-only (default): omit these or leave ENABLE_API unset.
# Quote + booking forms validate and show success without calling Django.

# When your Django API is running, enable submissions:
# NEXT_PUBLIC_ENABLE_API=true
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
